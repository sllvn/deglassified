!!!
%html
  %head
    %meta{ charset: 'utf-8' }
    %meta{ name: 'viewport', content:'width=device-width, initial-scale=1.0' }
    %title Without Glass
    = stylesheet_link_tag 'application', media: 'all'
    = javascript_include_tag 'vendor/modernizr'
    = csrf_meta_tags
  %body
    #sidebar
      %h1 Without Glass
      %p.lead Cataloguing the businesses that value your privacy.
      = yield
      .panel
        %p Know something we don&rsquo;t?
        %p
          %a(href='#') 
          = link_to 'Share your local Glass-free businesses.', '#', class: 'add-location', 'data-reveal-id' => 'add-location-modal'
          #add-location-modal.reveal-modal{'data-reveal' => true}
            %h2 Add Location
            .panel Let users login with site-specific login or OAuth (for maximum irony, use Google as OAuth provider).
            %a.close-reveal-modal &#215;
    #map
    = javascript_include_tag 'application'
